# ---------- Bí mật: KHÔNG BAO GIỜ commit ----------
backend/.env
.env.local
firmware/esp8266_room01/secrets.h

# ---------- Môi trường, thư viện ----------
backend/.venv/
__pycache__/
*.pyc
backend/staticfiles/
frontend/node_modules/
frontend/dist/
*.local

# ---------- File trung gian khi dựng Word ----------
build/
~$*.docx
*.asd

# ---------- Cấu hình riêng của máy ----------
.claude/settings.local.json

# ---------- Tư liệu riêng, không đưa lên kho ----------
# 12 ảnh chụp bảng lúc giao đề và payload mẫu để thử mosquitto_pub:
# vẫn nằm trên máy, chỉ là bày ra ở thư mục gốc của kho thì rối mắt.
/*.jpg
/*.json
