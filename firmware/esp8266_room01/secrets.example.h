#pragma once
// Chép file này thành `secrets.h` (cùng thư mục) rồi điền giá trị thật.
// `secrets.h` nằm trong .gitignore nên không bị đẩy lên GitHub.

const char* WIFI_SSID = "ten-wifi";
const char* WIFI_PASS = "mat-khau-wifi";
const char* MQTT_USER = "iotuser";       // tài khoản khai trong mosquitto passwd
const char* MQTT_PASS = "mat-khau-mqtt";
